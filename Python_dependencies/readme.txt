The requirements.txt file contains the set of packages that are installed in system and must be updateed before system migration using command "pip freeze > requirements.txt" which would generate the "requirements.txt" file using that file and command "pip install -r requirements.txt" the python packages can be installed .
